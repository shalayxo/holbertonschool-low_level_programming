#include "main.h"
#include <stdio.h>

/**
 * _abs - CHECK YOUR PROTOTYPE
 * @x: BLAH
 * Return: CHECK YOUR PROTOTYPE
 */
int _abs(int x)
{
	if (x >= 0)
	{
		return (x);
	}
	else if (x < 0)
	{
		return (-x);
	}
	else
	{
		return (9);
	}
}
