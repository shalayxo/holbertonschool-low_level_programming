#include "main.h"
#include <stdio.h>
/**
 * _isupper - checks uppercase
 * @c: c checks upper letter
 *
 * Return: uppercase returns
 */
int _isupper(int c)
{
	if (c >= 'A'; &&c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
